---
phase: 05.2-refactor-working-directory-agent-logic-better-subagents-skills-and-harness
verified: 2026-04-04T11:03:21Z
status: passed
score: 8/8 must-haves verified
re_verification: false
---

# Phase 05.2: Refactor Working Directory, Agent Logic, Better Subagents, Skills and Harness — Verification Report

**Phase Goal:** Refactor existing components — no new capabilities. Swap FilesystemBackend for LocalShellBackend with env-var-driven root path, redesign search-agent to write evidence to scratch files instead of relaying inline, wire 3 pre-authored skill bundles via SkillsMiddleware.
**Verified:** 2026-04-04T11:03:21Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | harness.py uses LocalShellBackend (not FilesystemBackend) for scratch backend, driven by SCRATCH_DIR env var | VERIFIED | harness.py line 218: `backend = LocalShellBackend(root_dir=SCRATCH_DIR, ...)`; line 24: `os.environ.get("SCRATCH_DIR", ...)` |
| 2  | scratch.py SCRATCH_ROOT reads the same SCRATCH_DIR env var (same name and same default) | VERIFIED | scratch.py line 28: `SCRATCH_ROOT = Path(os.environ.get("SCRATCH_DIR", str(_DEFAULT_SCRATCH)))` — matches harness.py line 24 exactly |
| 3  | SkillsMiddleware is wired to orchestrator only (not search-agent or verifier middleware lists) | VERIFIED | search_middleware (lines 247-255) and verifier_middleware (lines 273-281) contain no SkillsMiddleware; orchestrator_middleware (line 302) does |
| 4  | SkillsMiddleware sources include quant-stats, cpi-inflation-adjuster, historical-fx | VERIFIED | harness.py lines 228-230: `sources=["/quant-stats/", "/cpi-inflation-adjuster/", "/historical-fx/"]` |
| 5  | create_harness_agent() takes no parameters (skills param removed) | VERIFIED | harness.py line 189: `def create_harness_agent():` — zero parameters |
| 6  | SEARCH_AGENT_SYSTEM_PROMPT contains UID extraction directive, file-writing instructions, pointer-only return | VERIFIED | Lines 130-132: UID extraction; lines 158-177: evidence/tables/extracted_values write instructions; lines 174-177: "Return ONLY a completion pointer" and "NEVER relay raw corpus text..." |
| 7  | ORCHESTRATOR_SYSTEM_PROMPT includes "UID: {uid} |" prefix pattern in search-agent task call examples | VERIFIED | harness.py lines 76-80: explicit call format and concrete example `UID: UID0001 | Task: ...` |
| 8  | agent.py SYSTEM_PROMPT is synced with harness.py ORCHESTRATOR_SYSTEM_PROMPT (both contain UID propagation) | VERIFIED | Programmatic comparison: both strings are 2454 chars and char-for-char identical |

**Score:** 8/8 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `workspace/src/harness.py` | LocalShellBackend scratch, SkillsMiddleware wired | VERIFIED | 347 lines, fully implemented |
| `workspace/src/scratch.py` | SCRATCH_ROOT reads SCRATCH_DIR env var | VERIFIED | Line 28 uses `os.environ.get("SCRATCH_DIR", ...)` |
| `workspace/src/agent.py` | SYSTEM_PROMPT synced with harness ORCHESTRATOR_SYSTEM_PROMPT | VERIFIED | Prompts are identical (2454 chars each) |
| `skills/quant-stats/` | Pre-authored skill bundle directory | VERIFIED | Directory exists at project root |
| `skills/cpi-inflation-adjuster/` | Pre-authored skill bundle directory | VERIFIED | Directory exists at project root |
| `skills/historical-fx/` | Pre-authored skill bundle directory | VERIFIED | Directory exists at project root |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `harness.py` SCRATCH_DIR env | `LocalShellBackend` | `root_dir=SCRATCH_DIR` (line 219) | WIRED | Env-var value flows directly into backend constructor |
| `harness.py` skills_mw | `orchestrator_middleware` | list insertion at index 1 (line 302) | WIRED | `skills_mw` in orchestrator list; absent from search and verifier lists |
| `scratch.py` SCRATCH_ROOT | same SCRATCH_DIR env var | `os.environ.get("SCRATCH_DIR", ...)` | WIRED | Same variable name and identical default path construction |
| `agent.py` SYSTEM_PROMPT | `harness.ORCHESTRATOR_SYSTEM_PROMPT` | copy-sync (content match) | WIRED | Confirmed identical by programmatic comparison |
| `SEARCH_AGENT_SYSTEM_PROMPT` | search_subagent spec | `"system_prompt": SEARCH_AGENT_SYSTEM_PROMPT` (line 265) | WIRED | Correctly referenced in subagent dict |

---

### Requirements Coverage

No REQUIREMENTS.md rows were mapped specifically to phase 05.2. Coverage is inferred from the 8 must-haves above, all of which map directly to the phase goal statement.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `harness.py` | 296 | Dead comment `###CAN ADD GENERAL SUBAGENT AS WELL..abs` | Info | No functional impact |
| `harness.py` | 315-320 | Commented-out middleware stubs (`##if middleware:`, `# if memory`) | Info | Leftover scaffolding, not blocking |

No blockers or warnings found. Both flagged items are inert comments with no functional effect.

---

### Human Verification Required

None. All must-haves are verifiable programmatically. The refactor is a pure code change with no visual, real-time, or external-service behavior that requires human observation.

---

### Gaps Summary

No gaps found. All 8 must-haves pass at all three verification levels (exists, substantive, wired).

---

## Additional Observations

1. `FilesystemBackend` remains imported in harness.py (line 208) and is legitimately used for the skills backend (line 226), not the scratch backend. This is correct per design — skills are read-only filesystem bundles, not shell-writable scratch space.

2. The default fallback path `_PROJECT_ROOT / "agentspace" / "scratch"` is identical between `harness.py` (line 23) and `scratch.py` (line 27), so both modules agree on the scratch location even without the env var set.

3. `create_harness_agent()` has no parameters (must-have 5 confirmed). Callers in `agent.py` (`run_question` line 133, `run_question_with_messages` line 166) invoke it as `harness.create_harness_agent()` with no arguments — consistent.

---

_Verified: 2026-04-04T11:03:21Z_
_Verifier: Claude (gsd-verifier)_
