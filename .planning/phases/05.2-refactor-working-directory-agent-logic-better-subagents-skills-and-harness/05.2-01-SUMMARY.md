---
phase: 05.2-refactor-working-directory-agent-logic-better-subagents-skills-and-harness
plan: 01
subsystem: harness
tags: [LocalShellBackend, SkillsMiddleware, env-var, scratch-directory, deepagents]

# Dependency graph
requires:
  - phase: 05.1.1-custom-deepagents-harness
    provides: harness.py with FilesystemBackend, SubAgentMiddleware, search/verifier subagents
provides:
  - LocalShellBackend with env-var-driven SCRATCH_DIR (portable, no hardcoded paths)
  - SkillsMiddleware on orchestrator with dedicated FilesystemBackend for 3 skill bundles
  - scratch.py SCRATCH_ROOT aligned with harness backend root
affects: [05.2-02-PLAN, phase-06]

# Tech tracking
tech-stack:
  added: [LocalShellBackend, SkillsMiddleware]
  patterns: [env-var-driven path resolution with computed default, separate backend per concern]

key-files:
  created: []
  modified:
    - workspace/src/harness.py
    - workspace/src/scratch.py

key-decisions:
  - "SkillsMiddleware on orchestrator only -- search-agent and verifier are pure workers"
  - "Separate FilesystemBackend for skills dir -- skills are read-only, no shell needed"
  - "SCRATCH_DIR env var with _PROJECT_ROOT / agentspace / scratch default -- matches CORPUS_DIR pattern"

patterns-established:
  - "Backend-per-concern: scratch uses LocalShellBackend, skills uses FilesystemBackend"
  - "Module-level path constants: SCRATCH_DIR exported for use by other modules"

# Metrics
duration: 2min
completed: 2026-04-04
---

# Phase 5.2 Plan 01: Backend Swap and Skills Wiring Summary

**LocalShellBackend with env-var SCRATCH_DIR replaces hardcoded Windows path; SkillsMiddleware wired to orchestrator only with dedicated skills backend**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-04T10:54:40Z
- **Completed:** 2026-04-04T10:56:25Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Replaced hardcoded `C:/Users/mdado/...` FilesystemBackend with portable LocalShellBackend using SCRATCH_DIR env var
- Wired SkillsMiddleware to orchestrator only with dedicated FilesystemBackend for 3 skill bundles (quant-stats, cpi-inflation-adjuster, historical-fx)
- Fixed scratch.py SCRATCH_ROOT divergence bug (was resolving to workspace/scratch instead of agentspace/scratch)
- Removed skills parameter from create_harness_agent() -- skills always discovered from directory

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace FilesystemBackend with LocalShellBackend and wire SkillsMiddleware** - `4751b10` (feat)
2. **Task 2: Fix scratch.py SCRATCH_ROOT to use SCRATCH_DIR env var** - `095e4cf` (fix)

## Files Created/Modified
- `workspace/src/harness.py` - Backend swap to LocalShellBackend, SkillsMiddleware on orchestrator, removed skills param
- `workspace/src/scratch.py` - SCRATCH_ROOT now reads SCRATCH_DIR env var with agentspace/scratch default

## Decisions Made
- SkillsMiddleware placed on orchestrator only (not search-agent or verifier) per Claudie principle: orchestrator reasons and delegates, workers execute
- Separate FilesystemBackend for skills directory (read-only) rather than sharing LocalShellBackend with scratch
- SCRATCH_DIR env var follows same pattern as CORPUS_DIR in config.py

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- harness.py ready for Plan 02 (search-agent system prompt refactor for file-writing pattern)
- LocalShellBackend provides `execute` tool for agents to run skill scripts
- Skills backend discovers 3 bundles from skills/ directory

---
*Phase: 05.2-refactor-working-directory-agent-logic-better-subagents-skills-and-harness*
*Completed: 2026-04-04*
