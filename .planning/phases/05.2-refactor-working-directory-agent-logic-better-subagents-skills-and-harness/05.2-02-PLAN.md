---
phase: 05.2-refactor-working-directory-agent-logic-better-subagents-skills-and-harness
plan: 02
type: execute
wave: 2
depends_on: ["05.2-01"]
files_modified:
  - workspace/src/harness.py
  - workspace/src/agent.py
autonomous: true

must_haves:
  truths:
    - "Search-agent writes evidence and extracted values to UID scratch files instead of returning data inline"
    - "Orchestrator receives file pointers from search-agent, not raw corpus data"
    - "Orchestrator system prompt instructs UID propagation in every search-agent task call"
    - "Search-agent system prompt shows how to parse UID prefix and write to UID scratch files"
    - "agent.py SYSTEM_PROMPT stays in sync with harness.py ORCHESTRATOR_SYSTEM_PROMPT"
  artifacts:
    - path: "workspace/src/harness.py"
      provides: "Updated ORCHESTRATOR_SYSTEM_PROMPT and SEARCH_AGENT_SYSTEM_PROMPT"
      contains: "UID:"
    - path: "workspace/src/agent.py"
      provides: "Synced SYSTEM_PROMPT with harness orchestrator prompt"
      contains: "UID:"
  key_links:
    - from: "workspace/src/harness.py (ORCHESTRATOR_SYSTEM_PROMPT)"
      to: "workspace/src/harness.py (SEARCH_AGENT_SYSTEM_PROMPT)"
      via: "UID propagation in task() call description"
      pattern: "UID:.*\\{uid\\}"
    - from: "workspace/src/harness.py (SEARCH_AGENT_SYSTEM_PROMPT)"
      to: "scratch filesystem"
      via: "write_file to {uid}/evidence.txt"
      pattern: "evidence\\.txt"
    - from: "workspace/src/agent.py (SYSTEM_PROMPT)"
      to: "workspace/src/harness.py (ORCHESTRATOR_SYSTEM_PROMPT)"
      via: "manual sync"
      pattern: "search-agent"
---

<objective>
Redesign the search-agent to write evidence to scratch files instead of returning data inline, and update orchestrator/agent prompts for UID propagation and file-pointer-based reasoning.

Purpose: This is the key architectural change of Phase 5.2 — the Claudie principle. Raw BM25 span text must stay out of the orchestrator's context window. The search-agent dumps evidence to disk; the orchestrator reasons from file references only.

Output: Updated system prompts in harness.py + synced agent.py SYSTEM_PROMPT.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-refactor-working-directory-agent-logic-better-subagents-skills-and-harness/05.2-RESEARCH.md
@.planning/phases/05.2-refactor-working-directory-agent-logic-better-subagents-skills-and-harness/05.2-01-SUMMARY.md
@workspace/src/harness.py
@workspace/src/agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update search-agent system prompt for file-writing pattern</name>
  <files>workspace/src/harness.py</files>
  <action>
Replace the existing `SEARCH_AGENT_SYSTEM_PROMPT` in harness.py with a version that:

**1. Adds UID extraction directive (new section at top of prompt):**
```
## UID Extraction (MANDATORY)
The task description begins with 'UID: <uid> | Task: ...'.
Extract the UID from this prefix. ALL scratch file paths use this UID:
  {uid}/evidence.txt, {uid}/tables.txt, {uid}/extracted_values.txt
```

**2. Changes "Evidence Recording" to mandate file writes (not just appending):**
Replace the existing evidence recording section with:
```
## Evidence Recording (MANDATORY)
After EACH search_in_file call, WRITE (append) to {uid}/evidence.txt:
  Source: {file_path}
  {span_text}
  Note: {why this span was selected}
  ---

After EACH extract_table_block call, WRITE (append) to {uid}/tables.txt:
  Source: {file_path}
  {table_block}
  ---

After ALL searches and extractions complete, write to {uid}/extracted_values.txt:
  Format: one line per value: variable_name = value (unit), source: filename
  Example: defense_1940 = 2602 (millions), source: treasury_bulletin_1940_03.txt
```

**3. Changes "Return Format" to return ONLY a file pointer:**
Replace the existing return format section with:
```
## Return Format
Return ONLY a completion pointer. Do NOT include raw data or numeric values:
  "Evidence written to {uid}/evidence.txt. Extracted values written to {uid}/extracted_values.txt."

NEVER relay raw corpus text, table blocks, or numeric values inline back to the orchestrator.
The orchestrator will read files directly — do not summarize or repeat file contents.
```

Keep the existing "Your Role", "Fiscal Year Adjacency Rule", and "Parallel Search Rule" sections unchanged.
  </action>
  <verify>
Run from workspace/ directory:
```
.venv/Scripts/python.exe -c "
from src.harness import SEARCH_AGENT_SYSTEM_PROMPT
assert 'UID:' in SEARCH_AGENT_SYSTEM_PROMPT, 'Missing UID extraction'
assert 'NEVER relay' in SEARCH_AGENT_SYSTEM_PROMPT, 'Missing no-relay directive'
assert 'completion pointer' in SEARCH_AGENT_SYSTEM_PROMPT.lower() or 'pointer' in SEARCH_AGENT_SYSTEM_PROMPT.lower(), 'Missing pointer return'
print('Search prompt OK')
"
```
  </verify>
  <done>
Search-agent system prompt instructs: extract UID from task description, write evidence/tables/values to UID scratch files, return only a file pointer to orchestrator.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update orchestrator prompt for UID propagation and file-pointer reasoning</name>
  <files>workspace/src/harness.py, workspace/src/agent.py</files>
  <action>
**In harness.py, update `ORCHESTRATOR_SYSTEM_PROMPT`:**

1. **Update "Retrieval via Search Agent" section** to include UID in every task call:
Replace the example with:
```
To retrieve financial data from the corpus, call the search agent with the UID prefix:
  task(subagent_type='search-agent', description='UID: {uid} | Task: <plain English task>')

Example: task(subagent_type='search-agent',
              description='UID: UID0001 | Task: Find defense expenditures for FY1940 across all relevant files')

IMPORTANT: Every search-agent call MUST include 'UID: {uid} |' at the start of the description.
The {uid} value comes from the question preamble ("Question UID: ...").

The search agent writes evidence to {uid}/evidence.txt and extracted values to {uid}/extracted_values.txt.
It returns ONLY a file pointer — do NOT expect inline data.
```

2. **Update "Scratch File Writing Instructions" section** to reflect that search-agent now owns evidence + extracted_values files:
Remove the instruction for the orchestrator to write `{uid}/extracted_values.txt` (search-agent does this now).
Keep the orchestrator's responsibility for `{uid}/calc.txt` and `{uid}/answer.txt`.
Add: "After the search-agent completes, read {uid}/extracted_values.txt to get the values for calculation."

3. **Update the "Verification" section** to also include UID in verifier calls:
```
Before calling normalize_answer, call the verifier with the UID prefix:
  task(subagent_type='verifier', description='UID: {uid} | <answer> | Evidence: <summary>')
```

**In agent.py, sync `SYSTEM_PROMPT`:**
Copy the updated `ORCHESTRATOR_SYSTEM_PROMPT` content from harness.py into agent.py's `SYSTEM_PROMPT` variable to keep them in sync (per Phase 05.1.1 decision: harness.py is source of truth, agent.py kept in sync manually).
  </action>
  <verify>
Run from workspace/ directory:
```
.venv/Scripts/python.exe -c "
from src.harness import ORCHESTRATOR_SYSTEM_PROMPT
from src.agent import SYSTEM_PROMPT
# Check UID propagation in orchestrator prompt
assert 'UID: {uid} | Task:' in ORCHESTRATOR_SYSTEM_PROMPT, 'Missing UID in search-agent call'
assert 'file pointer' in ORCHESTRATOR_SYSTEM_PROMPT.lower() or 'pointer' in ORCHESTRATOR_SYSTEM_PROMPT.lower(), 'Missing pointer expectation'
# Check sync
assert 'UID: {uid}' in SYSTEM_PROMPT, 'agent.py not synced'
print('Orchestrator + agent prompts OK')
"
```
  </verify>
  <done>
Orchestrator prompt includes UID in every search-agent and verifier task call, expects file pointers not inline data, and reads extracted_values.txt for calculation inputs. agent.py SYSTEM_PROMPT is synced.
  </done>
</task>

</tasks>

<verification>
1. `SEARCH_AGENT_SYSTEM_PROMPT` contains UID extraction directive, file-writing instructions, and pointer-only return format
2. `ORCHESTRATOR_SYSTEM_PROMPT` includes `UID: {uid} |` in search-agent and verifier task call examples
3. Orchestrator prompt does NOT instruct writing extracted_values.txt (search-agent owns that now)
4. Orchestrator prompt instructs reading {uid}/extracted_values.txt after search-agent completes
5. agent.py `SYSTEM_PROMPT` matches harness.py `ORCHESTRATOR_SYSTEM_PROMPT` content
6. No raw data relay patterns remain in search-agent return format instructions
</verification>

<success_criteria>
- Search-agent writes evidence, tables, and extracted values to UID scratch files via filesystem tools
- Search-agent returns only a completion pointer string, never raw corpus data
- Orchestrator propagates UID in every search-agent and verifier task() call description
- Orchestrator reads from extracted_values.txt for calculation inputs instead of parsing inline findings
- agent.py SYSTEM_PROMPT is synced with harness.py ORCHESTRATOR_SYSTEM_PROMPT
- The Claudie principle is enforced: subagents dump to files, orchestrator reasons from references
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-refactor-working-directory-agent-logic-better-subagents-skills-and-harness/05.2-02-SUMMARY.md`
</output>
