---
phase: 05.1.1-custom-deepagents-harness-graph-level-control-context-isolation-subagent-search-pipeline
verified: 2026-03-25T20:36:02Z
status: passed
score: 7/7 must-haves verified
re_verification: false
---

# Phase 05.1.1: Custom DeepAgents Harness Verification Report

**Phase Goal:** Replace `create_deep_agent()` with a custom harness (`harness.py`) that assembles the middleware stack manually and introduces a dedicated search subagent to isolate corpus retrieval from the orchestrator's context window.
**Verified:** 2026-03-25T20:36:02Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Calling `run_question(uid, question)` succeeds without ImportError or ValueError | VERIFIED | `agent.py` imports `from src import harness` at module level; both `run_question` and `run_question_with_messages` call `harness.create_harness_agent()`. All dependency imports in harness are deferred inside the function body, preventing import-time failures. `model_adapter.get_model()` raises `ValueError` only for unsupported model prefixes — "gemini-" and "claude-" are both handled. |
| 2 | The orchestrator tool list contains only `calculate`, `pct_change`, `sum_values`, `normalize_answer` — no retrieval tools | VERIFIED | `harness.py` line 247: `tools=[calculate, pct_change, sum_values, normalize_answer]`. `route_files`, `search_in_file`, and `extract_table_block` are imported under Step 2 comment and assigned only to `search_subagent["tools"]` (line 200). No retrieval tool appears in the `create_agent()` call. |
| 3 | The search subagent (`name='search-agent'`) owns `route_files`, `search_in_file`, `extract_table_block` | VERIFIED | `harness.py` lines 190–202: `search_subagent` dict has `"name": "search-agent"` and `"tools": [route_files, search_in_file, extract_table_block]`. All three tool files exist under `workspace/src/tools/`. |
| 4 | The verifier subagent (`name='verifier'`) is registered via `SubAgentMiddleware` (VER-01) | VERIFIED | `harness.py` line 215: `"name": "verifier"`. `SubAgentMiddleware` at lines 230–234 receives `subagents=[search_subagent, verifier_subagent]`. Both subagents have `model` and `tools` keys present, satisfying the SubAgent TypedDict requirement. |
| 5 | `SYSTEM_PROMPT` in `agent.py` contains no FY Adjacency Rule or Parallel Search Rule | VERIFIED | Grep for "Fiscal Year Adjacency", "Parallel Search Rule", "FY Adjacency" in `agent.py` returns no matches. `agent.py` SYSTEM_PROMPT (lines 40–99) contains no trace of either rule. |
| 6 | `SYSTEM_PROMPT` in `agent.py` instructs the orchestrator to use `task(subagent_type='search-agent', ...)` for retrieval | VERIFIED | `agent.py` lines 52–53: `task(subagent_type='search-agent', description='<plain English task>')` appears in the "Retrieval via Search Agent" section. Line 62: `Do NOT call route_files, search_in_file, or extract_table_block directly.` |
| 7 | `harness.py` appends `BASE_AGENT_PROMPT` to the orchestrator system prompt before calling `create_agent()` | VERIFIED | `harness.py` line 154: `from deepagents.graph import BASE_AGENT_PROMPT`. Line 240: `final_system_prompt = ORCHESTRATOR_SYSTEM_PROMPT + "\n\n" + BASE_AGENT_PROMPT`. This concatenated prompt is passed to `create_agent()` at line 249. |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `workspace/src/harness.py` | `create_harness_agent()` function returning compiled agent | VERIFIED | 253-line file. Exports `ORCHESTRATOR_SYSTEM_PROMPT`, `SEARCH_AGENT_SYSTEM_PROMPT`, `create_harness_agent`. Full middleware assembly including SubAgentMiddleware, all middleware layers in correct order. |
| `workspace/src/agent.py` | Trimmed `SYSTEM_PROMPT` + `run_question` entry point using harness | VERIFIED | 183-line file. `SYSTEM_PROMPT` spans lines 40–99, contains Retrieval via Search Agent and Verification sections. Both `run_question` and `run_question_with_messages` call `harness.create_harness_agent()`. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `agent.py` | `harness.py` | `import harness; harness.create_harness_agent()` | WIRED | `agent.py` line 22: `from src import harness`. Lines 133 and 166: `agent = harness.create_harness_agent()`. |
| `harness.py` | `SubAgentMiddleware` | `SubAgentMiddleware(backend=backend, subagents=[...])` | WIRED | Line 150: import from `deepagents.middleware.subagents`. Lines 230–234: `SubAgentMiddleware(backend=backend, subagents=[search_subagent, verifier_subagent])`. |
| `harness.py` | `BASE_AGENT_PROMPT` | `ORCHESTRATOR_SYSTEM_PROMPT + "\n\n" + BASE_AGENT_PROMPT` | WIRED | Line 154: import from `deepagents.graph`. Line 240: concatenation before `create_agent()` call. |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | No TODOs, FIXMEs, stub returns, or empty handlers found in either harness.py or agent.py. |

---

### Notable Deviation (Non-blocking)

The PLAN (Task 1, Step 4) prescribed `from src.tools.verifier import verify_answer` as the verifier's tool. The file `verifier.py` exports no `verify_answer` function — it exports `VERIFIER_SYSTEM_PROMPT` and `VERIFIER_SUBAGENT_SPEC` (which uses `calculate` as its tool). The harness correctly imports `VERIFIER_SYSTEM_PROMPT` and assigns `calculate` (aliased as `calculate_for_verifier`) as the verifier's tool. This matches `VERIFIER_SUBAGENT_SPEC["tools"]` in `verifier.py` and is functionally correct. The plan text was aspirational; the implementation self-corrected to match the actual module API.

This deviation does NOT affect any must-have: the verifier subagent IS registered with `name="verifier"` via `SubAgentMiddleware`, satisfying truth 4.

---

### Human Verification Required

None identified. All must-haves are statically verifiable from the source code. Runtime smoke test was documented in the SUMMARY as passing (returning `CompiledStateGraph` without exception) using `.venv/Scripts/python.exe`.

---

### Requirements Coverage

No `.planning/REQUIREMENTS.md` entries are mapped to this phase. Coverage check not applicable.

---

## Gaps Summary

No gaps. All 7 must-haves are fully verified against the actual codebase.

- `harness.py` exists, is substantive (253 lines), and is wired into `agent.py`
- `agent.py` is updated, is substantive (183 lines), and delegates agent construction to `harness`
- `create_deep_agent` no longer appears anywhere in active `src/` code (only in comments and docstrings)
- `SYSTEM_PROMPT` in `agent.py` is trimmed of FY and Parallel Search rules and contains search-agent delegation instructions
- The middleware stack correctly orders: `TodoListMiddleware` → `FilesystemMiddleware` → `SubAgentMiddleware` → `SummarizationMiddleware` → `PatchToolCallsMiddleware` → `AnthropicPromptCachingMiddleware`
- `BASE_AGENT_PROMPT` is explicitly appended before `create_agent()`, guarding against Pitfall 1

---

_Verified: 2026-03-25T20:36:02Z_
_Verifier: Claude (gsd-verifier)_
