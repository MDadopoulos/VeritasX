---
phase: 03.1-architecture-refactor
plan: 03
type: execute
wave: 3
depends_on: ["03.1-01", "03.1-02"]
files_modified:
  - workspace/tests/test_calculate.py
  - workspace/tests/test_route_files.py
  - workspace/tests/test_search_in_file.py
  - workspace/tests/test_extract_table_block.py
  - workspace/tests/test_normalize_answer.py
  - workspace/tests/test_model_adapter.py
  - workspace/tests/test_agent.py
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true

must_haves:
  truths:
    - "All unit tests pass with the new plain-function tool API"
    - "No test imports _impl functions or StructuredTool"
    - "Model-switch integration test exists and asserts both Gemini and Claude produce an answer"
    - "STATE.md and ROADMAP.md reflect the completed architecture refactor"
    - "All tests pass: pytest returns exit code 0 (excluding integration tests)"
  artifacts:
    - path: "workspace/tests/test_calculate.py"
      provides: "Calculator tests calling plain functions"
      contains: "from src.tools.calculate import calculate"
    - path: "workspace/tests/test_route_files.py"
      provides: "Route files tests with monkeypatched _CORPUS_DIR"
      contains: "monkeypatch"
    - path: "workspace/tests/test_agent.py"
      provides: "Integration tests including model-switch test"
      contains: "model_switch"
    - path: ".planning/STATE.md"
      provides: "Updated state reflecting Phase 3.1 completion"
      contains: "Phase 3.1"
  key_links:
    - from: "workspace/tests/test_calculate.py"
      to: "workspace/src/tools/calculate.py"
      via: "direct function import and call"
      pattern: "from src\\.tools\\.calculate import calculate"
    - from: "workspace/tests/test_route_files.py"
      to: "workspace/src/tools/route_files._CORPUS_DIR"
      via: "monkeypatch.setattr"
      pattern: "monkeypatch.*_CORPUS_DIR"
---

<objective>
Rewrite all test files to use the new plain-function API, add model-switch integration test, and update planning docs.

Purpose: Complete the refactor by ensuring all tests pass against the new architecture and planning state accurately reflects the new clean codebase.

Output: All tests green, model-switch test added, STATE.md and ROADMAP.md updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-architecture-refactor-deep-agent-native-tools-model-agnostic-pipeline/03.1-CONTEXT.md
@.planning/phases/03.1-architecture-refactor-deep-agent-native-tools-model-agnostic-pipeline/03.1-RESEARCH.md
@.planning/phases/03.1-architecture-refactor-deep-agent-native-tools-model-agnostic-pipeline/03.1-01-SUMMARY.md
@.planning/phases/03.1-architecture-refactor-deep-agent-native-tools-model-agnostic-pipeline/03.1-02-SUMMARY.md
@workspace/tests/test_calculate.py
@workspace/tests/test_route_files.py
@workspace/tests/test_search_in_file.py
@workspace/tests/test_extract_table_block.py
@workspace/tests/test_normalize_answer.py
@workspace/tests/test_model_adapter.py
@workspace/tests/test_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite tool unit tests for plain-function API</name>
  <files>
    workspace/tests/test_calculate.py
    workspace/tests/test_route_files.py
    workspace/tests/test_search_in_file.py
    workspace/tests/test_extract_table_block.py
    workspace/tests/test_normalize_answer.py
    workspace/tests/test_model_adapter.py
  </files>
  <action>
Rewrite each test file to call plain functions instead of `.run()` / `.invoke()` on StructuredTool instances.

**test_calculate.py:**
- Import: `from src.tools.calculate import calculate, pct_change, sum_values`
- Replace ALL `calculate.run("expr")` calls with `calculate("expr")`
- Replace ALL `pct_change.invoke({"old": ..., "new": ...})` calls with `pct_change(old=..., new=...)`
- Replace ALL `sum_values.invoke({"pairs": ..., "expected_count": ...})` calls with `sum_values(pairs=..., expected_count=...)`
- Remove any `from src.tools.calculate import _pct_change_impl` imports — call `pct_change(...)` directly instead
- The tests that previously called `_pct_change_impl("abc", 100)` for invalid input: call `pct_change(old="abc", new=100)` directly since there is no Pydantic validation layer anymore
- Same for `_pct_change_impl(100, 200, unit_old=None, unit_new=None)` -> `pct_change(100, 200, unit_old=None, unit_new=None)`
- All assertions remain the same (Decimal comparisons, error dict checks)

**test_route_files.py:**
- Import: `from src.tools.route_files import extract_years, fy_to_calendar_months, route_files, year_to_months` (no `_route_files_impl`)
- Remove `from src.config import Config` import
- Tests that previously called `_route_files_impl(question, config)` with a Config object: use `monkeypatch.setattr("src.tools.route_files._CORPUS_DIR", Path("/path/to/test/corpus"))` and then call `route_files(question)` directly
- Tests that called `route_files.run(question)` or `route_files.invoke({"question": ...})`: call `route_files(question)` directly
- All extract_years and fy_to_calendar_months tests stay unchanged (they were always plain functions)

**test_search_in_file.py:**
- Replace any `search_in_file.run(...)` or `search_in_file.invoke(...)` with direct `search_in_file(file_path, query)` calls
- Remove `_search_in_file_impl` imports if any

**test_extract_table_block.py:**
- Replace any `extract_table_block.run(...)` or `extract_table_block.invoke(...)` with direct `extract_table_block(span_text, anchor)` calls
- Remove `_extract_table_block_impl` imports if any

**test_normalize_answer.py:**
- Replace any `normalize_answer.run(...)` or `normalize_answer.invoke(...)` with direct `normalize_answer(raw)` calls
- Remove `_normalize_answer_impl` imports if any

**test_model_adapter.py:**
- Update to test `get_model(model_id)` instead of `get_model(config)` — use monkeypatch to set GOOGLE_CLOUD_PROJECT env var
- Remove Config-based test patterns

For ALL test files: remove any imports of `_impl` functions, StructuredTool, or Config (where no longer needed). All function calls should be direct Python calls, not `.run()` / `.invoke()`.
  </action>
  <verify>
Run `cd workspace && python -m pytest tests/ -m "not integration" --tb=short -q` — all tests must pass.
Run `grep -rn "_impl\|\.run(\|\.invoke(" workspace/tests/` — should return NO matches (except possibly in comments).
  </verify>
  <done>
All tool unit tests call plain functions directly. No _impl imports, no .run()/.invoke() calls, no StructuredTool references. All unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add model-switch integration test and update planning docs</name>
  <files>
    workspace/tests/test_agent.py
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <action>
**test_agent.py updates:**
- Update `run_question` and `run_question_with_messages` imports — they no longer accept `config` param
- Add a new integration test `test_model_switch_same_answer`:
  ```python
  @requires_vertex
  @pytest.mark.integration
  @pytest.mark.timeout(600)
  def test_model_switch_same_answer():
      """Same question with gemini and claude MODEL_ID produces a non-empty answer."""
      import os
      from src.agent import run_question

      uid_gemini = "UID_MODEL_SWITCH_GEMINI"
      uid_claude = "UID_MODEL_SWITCH_CLAUDE"
      question = Q1_QUESTION  # simple lookup

      # Run with Gemini
      os.environ["MODEL_ID"] = "gemini-2.0-flash"
      answer_gemini = run_question(uid_gemini, question)
      assert answer_gemini and len(answer_gemini.strip()) > 0, "Gemini produced empty answer"

      # Run with Claude
      os.environ["MODEL_ID"] = "claude-sonnet-4-6"
      answer_claude = run_question(uid_claude, question)
      assert answer_claude and len(answer_claude.strip()) > 0, "Claude produced empty answer"

      # Both should produce non-empty answers (exact match not required —
      # different models may phrase differently, but both should answer)
  ```
- Remove any `config` parameter passing from existing tests (they call `run_question(uid, question)` which no longer takes config)

**STATE.md:**
- Update "Current Position" to reflect Phase 3.1 planning complete
- Under "Decisions", add a new section noting Phase 3.1 supersedes Phase 3 workaround decisions
- Remove or mark as superseded: the _impl, StructuredTool, TYPE_CHECKING, Optional[str], and retrieval_wrappers decisions
- Add new decisions:
  - [Phase 03.1]: Tools exported as plain functions — no @tool decorator, no StructuredTool
  - [Phase 03.1]: route_files reads CORPUS_DIR from env via module-level _CORPUS_DIR constant
  - [Phase 03.1]: get_model reads MODEL_ID from env directly, no Config param
  - [Phase 03.1]: retrieval_wrappers.py deleted — Deep Agents max-turns replaces counter wrappers
  - [Phase 03.1]: SYSTEM_PROMPT rewritten without RETRIEVAL_EXHAUSTED references

**ROADMAP.md:**
- Update Phase 03.1 entry:
  - Goal: "Refactor tool registration and agent wiring so all tools run as native Deep Agent tools, pipeline works with Gemini and Claude via MODEL_ID"
  - Plans: 3 plans
  - Plan list:
    - 03.1-01-PLAN.md — Rewrite 5 tool modules as plain functions, delete retrieval_wrappers.py
    - 03.1-02-PLAN.md — Rewrite model_adapter + agent.py for env-based config
    - 03.1-03-PLAN.md — Rewrite tests, add model-switch test, update planning docs
  </action>
  <verify>
Run `cd workspace && python -m pytest tests/ -m "not integration" --tb=short -q` — all non-integration tests pass.
`grep -n "_impl\|StructuredTool\|\.run(\|\.invoke(" workspace/tests/test_agent.py` returns NO matches.
STATE.md contains "Phase 3.1" references and superseded decisions are marked.
ROADMAP.md Phase 03.1 has Goal and 3 plans listed.
  </verify>
  <done>
Model-switch integration test added. All non-integration tests pass. STATE.md and ROADMAP.md updated to reflect the architecture refactor. No legacy patterns remain anywhere in the codebase.
  </done>
</task>

</tasks>

<verification>
1. `cd workspace && python -m pytest tests/ -m "not integration" --tb=short -q` — ALL unit tests pass, exit code 0
2. `grep -rn "_impl" workspace/src/ workspace/tests/` — NO matches (excluding __pycache__)
3. `grep -rn "StructuredTool\|from langchain_core.tools" workspace/src/ workspace/tests/` — NO matches
4. `grep -rn "retrieval_wrappers\|RETRIEVAL_EXHAUSTED\|RETRIEVAL_LIMIT" workspace/src/ workspace/tests/` — NO matches
5. STATE.md updated with Phase 3.1 decisions
6. ROADMAP.md Phase 03.1 has goal and 3 plans
</verification>

<success_criteria>
- All unit tests pass against the new plain-function API
- Model-switch integration test exists (runs only with Vertex AI credentials)
- No legacy patterns (_impl, StructuredTool, .run(), .invoke(), Config in tools/agent) anywhere in src/ or tests/
- STATE.md and ROADMAP.md accurately reflect the completed refactor
- Definition of done items 3 and 4 from CONTEXT.md are verifiable: all tests pass, no legacy patterns remain
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-architecture-refactor-deep-agent-native-tools-model-agnostic-pipeline/03.1-03-SUMMARY.md`
</output>
