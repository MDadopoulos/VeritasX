---
phase: 03.1-architecture-refactor
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - workspace/src/model_adapter.py
  - workspace/src/agent.py
autonomous: true

must_haves:
  truths:
    - "get_model reads MODEL_ID from os.environ directly, not from Config"
    - "create_agent passes plain tool functions to create_deep_agent, not StructuredTool instances"
    - "SYSTEM_PROMPT does not reference RETRIEVAL_EXHAUSTED"
    - "agent.py does not import from retrieval_wrappers"
    - "run_question and run_question_with_messages do not accept a config parameter"
  artifacts:
    - path: "workspace/src/model_adapter.py"
      provides: "get_model(model_id: str) factory"
      contains: "def get_model"
      exports: ["get_model"]
    - path: "workspace/src/agent.py"
      provides: "create_agent, run_question, run_question_with_messages"
      contains: "create_deep_agent"
      exports: ["create_agent", "run_question", "run_question_with_messages", "SYSTEM_PROMPT"]
  key_links:
    - from: "workspace/src/agent.py"
      to: "workspace/src/model_adapter.py"
      via: "get_model() call"
      pattern: "get_model"
    - from: "workspace/src/agent.py"
      to: "workspace/src/tools/*.py"
      via: "import plain functions"
      pattern: "from src\\.tools\\."
    - from: "workspace/src/model_adapter.py"
      to: "MODEL_ID env var"
      via: "os.environ.get"
      pattern: "os\\.environ\\.get.*MODEL_ID"
---

<objective>
Rewrite model_adapter.py and agent.py to use the new plain-function tool pattern and read MODEL_ID from environment directly.

Purpose: Complete the architecture refactor by wiring the clean tool functions into create_deep_agent, removing all Config threading, counter wrappers, and RETRIEVAL_EXHAUSTED references.

Output: Clean agent.py and model_adapter.py with no legacy patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-architecture-refactor-deep-agent-native-tools-model-agnostic-pipeline/03.1-CONTEXT.md
@.planning/phases/03.1-architecture-refactor-deep-agent-native-tools-model-agnostic-pipeline/03.1-RESEARCH.md
@.planning/phases/03.1-architecture-refactor-deep-agent-native-tools-model-agnostic-pipeline/03.1-01-SUMMARY.md
@workspace/src/model_adapter.py
@workspace/src/agent.py
@workspace/src/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite model_adapter.py with env-based get_model</name>
  <files>workspace/src/model_adapter.py</files>
  <action>
Rewrite `get_model` to accept `model_id: str | None = None` instead of `config: Config | None = None`.

New signature: `def get_model(model_id: str | None = None)`

Behavior:
- If `model_id` is None, read from `os.environ.get("MODEL_ID", "gemini-2.0-flash")`
- For project/location, read from `os.environ.get("GOOGLE_CLOUD_PROJECT", "")` and `os.environ.get("GOOGLE_CLOUD_LOCATION", "global")` directly
- If `model_id.startswith("gemini-")`: return `ChatGoogleGenerativeAI(model=model_id, project=project, location=location)`
- If `model_id.startswith("claude-")`: return `ChatAnthropicVertex(model_name=model_id, project=project, location=location)` — import from `langchain_google_vertexai.model_garden`
- Otherwise: raise `ValueError`

Remove all Config imports and TYPE_CHECKING blocks. The function reads env vars directly.
  </action>
  <verify>
`python -c "import os; os.environ['GOOGLE_CLOUD_PROJECT']='test'; from src.model_adapter import get_model; print(type(get_model))"` prints `<class 'function'>`.
`grep -n "Config" workspace/src/model_adapter.py` returns NO matches.
  </verify>
  <done>
get_model accepts optional model_id string, reads MODEL_ID + GCP vars from environment directly. No Config dependency.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite agent.py — clean agent factory and system prompt</name>
  <files>workspace/src/agent.py</files>
  <action>
Complete rewrite of agent.py. The file should have these components:

**1. SYSTEM_PROMPT** — Rewrite to match native tool names. Key changes:
- Remove the "When route_files or search_in_file returns RETRIEVAL_EXHAUSTED, STOP calling that tool" paragraph entirely
- Remove "Attempt to answer from evidence gathered so far" and the "I cannot determine" fallback tied to RETRIEVAL_EXHAUSTED
- Keep all scratch file writing instructions (evidence.txt, tables.txt, extracted_values.txt, calc.txt, verification.txt, answer.txt)
- Keep the mandatory planning gate (write_todos before retrieval)
- Keep tool usage rules (NEVER compute pct change inline, ALWAYS use calculate)
- Update tool references to match plain function names (they should be the same: route_files, search_in_file, etc.)

**2. Remove RETRIEVAL_LIMIT constant** — no longer needed (Deep Agents handles max-turns natively)

**3. create_agent()** — New signature: `def create_agent()` (no config param)
- Import `get_model` from `src.model_adapter` and call `get_model()` (no args — reads MODEL_ID from env)
- Import all 7 tool functions as plain functions:
  - `from src.tools.route_files import route_files`
  - `from src.tools.search_in_file import search_in_file`
  - `from src.tools.extract_table_block import extract_table_block`
  - `from src.tools.calculate import calculate, pct_change, sum_values`
  - `from src.tools.normalize_answer import normalize_answer`
- Pass them directly to `create_deep_agent(model=model, tools=[route_files, search_in_file, extract_table_block, calculate, pct_change, sum_values, normalize_answer], system_prompt=SYSTEM_PROMPT, ...)`
- Keep `FilesystemBackend(root_dir="./scratch", virtual_mode=False)` and `MemorySaver()`
- Do NOT import from `retrieval_wrappers` (deleted)

**4. run_question()** — New signature: `def run_question(uid: str, question: str) -> str` (no config param)
- Keep the same lifecycle: prepare_scratch -> create_agent -> invoke
- Keep UID preamble in user message
- Keep thread_id=uid in configurable

**5. run_question_with_messages()** — New signature: `def run_question_with_messages(uid: str, question: str) -> dict` (no config param)
- Same pattern as run_question but returns {"answer": str, "messages": list}

Remove all TYPE_CHECKING blocks, Config imports, and config parameters throughout.
  </action>
  <verify>
`python -c "from src.agent import SYSTEM_PROMPT; assert 'RETRIEVAL_EXHAUSTED' not in SYSTEM_PROMPT; print('SYSTEM_PROMPT clean')"` prints "SYSTEM_PROMPT clean".
`grep -n "Config\|config\|retrieval_wrappers\|RETRIEVAL_LIMIT\|_impl\|StructuredTool" workspace/src/agent.py` returns NO matches (except possibly "config" in comments or configurable dict key).
`grep -n "RETRIEVAL_EXHAUSTED" workspace/src/agent.py` returns NO matches.
  </verify>
  <done>
agent.py has clean create_agent (no config), clean run_question/run_question_with_messages (no config), rewritten SYSTEM_PROMPT without RETRIEVAL_EXHAUSTED, imports plain tool functions directly. No legacy patterns.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from src.agent import create_agent, run_question, SYSTEM_PROMPT; print('agent imports OK')"` succeeds
2. `python -c "from src.model_adapter import get_model; print('model_adapter imports OK')"` succeeds
3. No references to Config, retrieval_wrappers, RETRIEVAL_EXHAUSTED, _impl, or StructuredTool in either file
4. SYSTEM_PROMPT still contains scratch file instructions and planning gate
</verification>

<success_criteria>
- model_adapter.py: get_model reads MODEL_ID from env, no Config dependency
- agent.py: create_agent/run_question/run_question_with_messages have no config param
- SYSTEM_PROMPT rewritten without RETRIEVAL_EXHAUSTED, keeps scratch and planning instructions
- All tool imports use plain functions from rewritten modules
- No legacy patterns remain in either file
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-architecture-refactor-deep-agent-native-tools-model-agnostic-pipeline/03.1-02-SUMMARY.md`
</output>
