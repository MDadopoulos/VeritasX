---
phase: 03-agent-loop-scratch-space
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/requirements.txt
  - workspace/src/tools/route_files.py
  - workspace/src/tools/search_in_file.py
  - workspace/src/tools/extract_table_block.py
  - workspace/src/tools/calculate.py
  - workspace/src/tools/normalize_answer.py
  - workspace/src/scratch.py
autonomous: true

must_haves:
  truths:
    - "All 5 tool modules with public tool functions export @tool-decorated functions (classify_table_rows.py is an internal helper not registered with the agent)"
    - "The 7 public tool functions are: route_files, search_in_file, extract_table_block, calculate, pct_change, sum_values, normalize_answer"
    - "prepare_scratch(uid) wipes and recreates ./scratch/{uid}/ directory"
    - "prepare_scratch(uid) called twice for the same UID does not raise an error"
    - "deepagents==0.4.11 is installed and importable"
  artifacts:
    - path: "workspace/src/scratch.py"
      provides: "Scratch directory lifecycle helpers"
      exports: ["prepare_scratch"]
    - path: "workspace/requirements.txt"
      provides: "Updated dependencies including deepagents"
      contains: "deepagents==0.4.11"
  key_links:
    - from: "workspace/src/tools/route_files.py"
      to: "langchain_core.tools.tool"
      via: "@tool decorator"
      pattern: "@tool"
    - from: "workspace/src/scratch.py"
      to: "./scratch/{uid}/"
      via: "Path.mkdir + shutil.rmtree"
      pattern: "shutil\\.rmtree|mkdir"
---

<objective>
Install deepagents dependency, add @tool decorators to all Phase 1+2 tool functions so they are compatible with create_deep_agent, and create the scratch directory lifecycle module.

Purpose: Foundation wiring — create_deep_agent requires @tool-decorated functions (Pitfall 5 from research), and the scratch lifecycle must exist before the agent can write files (Pitfall 6).
Output: All tools decorated, scratch.py with prepare_scratch(), deepagents installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-loop-scratch-space/03-RESEARCH.md
@workspace/src/tools/route_files.py
@workspace/src/tools/search_in_file.py
@workspace/src/tools/extract_table_block.py
@workspace/src/tools/calculate.py
@workspace/src/tools/normalize_answer.py
@workspace/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deepagents and add @tool decorators to all tool functions</name>
  <files>
    workspace/requirements.txt
    workspace/src/tools/route_files.py
    workspace/src/tools/search_in_file.py
    workspace/src/tools/extract_table_block.py
    workspace/src/tools/calculate.py
    workspace/src/tools/normalize_answer.py
  </files>
  <action>
1. Add `deepagents==0.4.11` to `workspace/requirements.txt` and install it:
   ```
   cd workspace && uv add deepagents==0.4.11
   ```
   This pulls langchain==1.2.12, langgraph>=1.1.1, langchain-core>=1.2.18 as transitive deps.

2. **Go/no-go gate:** Before decorating all modules, validate the approach on ONE module first.
   - Add `from langchain_core.tools import tool` to `calculate.py`
   - Decorate ONLY `calculate` with `@tool(name="calculate")`
   - Run `python -m pytest tests/ -v -k calculate` to confirm existing tests still pass with the decorated function
   - If tests FAIL: the `StructuredTool.__call__` does not forward positional args as expected. Switch to the wrapper approach: keep the original function undecorated, create a `calculate_tool = tool(name="calculate")(calculate)` alias at module bottom, and export both. Then adapt this pattern for all modules.
   - If tests PASS: proceed to decorate all remaining functions with the direct `@tool` approach.

3. For each of the 5 tool modules (NOT classify_table_rows.py — it is an internal helper called by extract_table_block, not a tool the agent invokes directly), add `from langchain_core.tools import tool` and decorate the public tool functions with `@tool(name="...")`. The decorator goes on the PUBLIC functions only (not internal helpers like `_classify_label`, `_eval_node`, etc.).

   **classify_table_rows.py is explicitly excluded** — it is an internal helper used by extract_table_block.py, not registered as an agent tool. Do NOT decorate it.

   Functions to decorate (7 total across 5 modules):
   - `route_files.py`: `route_files` -> `@tool(name="route_files")`
   - `search_in_file.py`: `search_in_file` -> `@tool(name="search_in_file")`
   - `extract_table_block.py`: `extract_table_block` -> `@tool(name="extract_table_block")`
   - `calculate.py`: `calculate` -> `@tool(name="calculate")`, `pct_change` -> `@tool(name="pct_change")`, `sum_values` -> `@tool(name="sum_values")`
   - `normalize_answer.py`: `normalize_answer` -> `@tool(name="normalize_answer")`

4. IMPORTANT: The `@tool` decorator infers the tool schema from type hints and docstrings. Each decorated function must have:
   - Type hints on all parameters
   - A docstring (already present in all tools per Phase 1+2 patterns)
   - Return type annotation (`-> dict`)

5. Run the FULL existing test suite to confirm nothing is broken:
   ```
   cd workspace && python -m pytest tests/ -v
   ```
   All 361 tests must pass. If any fail due to call syntax changes from `@tool` decoration, fix the call syntax in the failing tests (e.g., change `route_files("question")` to `route_files.invoke({"question": "..."})` if needed).
  </action>
  <verify>
   - `python -c "from deepagents import create_deep_agent; print('OK')"` succeeds
   - `python -c "from src.tools.route_files import route_files; print(route_files.name)"` prints `route_files`
   - `python -c "from src.tools.calculate import calculate, pct_change, sum_values; print(calculate.name, pct_change.name, sum_values.name)"` prints the three names
   - `python -m pytest tests/ -v` — all 361 existing tests still pass
  </verify>
  <done>deepagents installed, all 7 public tool functions across 5 modules decorated with @tool (classify_table_rows.py excluded as internal helper), all existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create scratch.py with directory lifecycle helpers</name>
  <files>workspace/src/scratch.py</files>
  <action>
Create `workspace/src/scratch.py` with:

1. `SCRATCH_ROOT = Path("./scratch")` — module-level constant

2. `prepare_scratch(uid: str) -> Path`:
   - Validates uid is non-empty string, raises ValueError otherwise
   - Computes `scratch_dir = SCRATCH_ROOT / uid`
   - If `scratch_dir.exists()`: `shutil.rmtree(scratch_dir)` (SCR-01: overwrites prior run)
   - `scratch_dir.mkdir(parents=True)` (also creates ./scratch/ if missing)
   - Returns `scratch_dir` as Path

3. `SCRATCH_FILES` — list constant of the six expected filenames:
   `["evidence.txt", "tables.txt", "extracted_values.txt", "calc.txt", "verification.txt", "answer.txt"]`

4. `verify_scratch_complete(uid: str) -> dict`:
   - Checks that all six files in SCRATCH_FILES exist and are non-empty in `SCRATCH_ROOT / uid`
   - Returns `{"complete": True/False, "missing": [...], "empty": [...]}`
   - Used by smoke tests in Plan 03

Follow existing patterns: return dicts (never raise for expected conditions), input validation at top.
  </action>
  <verify>
   - `python -c "from src.scratch import prepare_scratch, verify_scratch_complete, SCRATCH_FILES; print(len(SCRATCH_FILES))"`  prints `6`
   - Create a quick inline test:
     ```
     python -c "
     from src.scratch import prepare_scratch, verify_scratch_complete
     p = prepare_scratch('test_uid')
     assert p.exists()
     r = verify_scratch_complete('test_uid')
     assert not r['complete']
     assert len(r['missing']) == 6
     # cleanup
     import shutil; shutil.rmtree(p)
     print('OK')
     "
     ```
  </verify>
  <done>scratch.py exists with prepare_scratch and verify_scratch_complete, both importable and functional</done>
</task>

</tasks>

<verification>
- `python -c "from deepagents import create_deep_agent"` — no ImportError
- `python -m pytest tests/ -v` — all 361 tests pass
- All 7 tool functions (across 5 modules) have `.name` attribute matching their function name
- classify_table_rows.py is NOT decorated (internal helper)
- `prepare_scratch` creates and returns a valid Path
</verification>

<success_criteria>
- deepagents==0.4.11 installed and importable
- All 7 public tool functions across 5 modules decorated with @tool(name=...) — classify_table_rows.py excluded as internal helper
- scratch.py module with prepare_scratch() and verify_scratch_complete()
- All existing tests pass (no regressions from @tool decoration)
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-loop-scratch-space/03-01-SUMMARY.md`
</output>
