---
phase: 05.1-enhance-agent-prompts-tools-and-agentbeats-a2a-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/src/agent.py
  - workspace/src/tools/route_files.py
  - workspace/src/tools/search_in_file.py
  - workspace/src/tools/calculate.py
  - workspace/src/tools/normalize_answer.py
  - workspace/src/tools/verifier.py
autonomous: true

must_haves:
  truths:
    - "SYSTEM_PROMPT contains the FY Adjacency Rule instructing the agent to call route_files twice for any FY N question (once for year N, once for year N+1)"
    - "SYSTEM_PROMPT contains the Parallel Search Rule instructing the agent to issue multiple search_in_file calls in a single turn"
    - "Each tool's docstring includes corpus structure hints visible to the LLM at tool-binding time"
    - "No 'cannot determine' give-up policy is duplicated in the system prompt (verifier handles this)"
  artifacts:
    - path: "workspace/src/agent.py"
      provides: "Updated SYSTEM_PROMPT with FY Adjacency Rule and Parallel Search Rule"
      contains: "Fiscal Year Adjacency Rule"
    - path: "workspace/src/tools/route_files.py"
      provides: "Corpus structure hints in route_files docstring"
      contains: "CORPUS STRUCTURE"
    - path: "workspace/src/tools/search_in_file.py"
      provides: "Parallel call guidance in search_in_file docstring"
      contains: "PARALLEL"
    - path: "workspace/src/tools/calculate.py"
      provides: "Usage hints in calculate docstring"
      contains: "CORPUS"
    - path: "workspace/src/tools/normalize_answer.py"
      provides: "Usage hints in normalize_answer docstring"
      contains: "verification_token"
    - path: "workspace/src/tools/verifier.py"
      provides: "Usage hints in verifier docstring"
      contains: "CORPUS"
  key_links:
    - from: "workspace/src/agent.py"
      to: "workspace/src/tools/route_files.py"
      via: "SYSTEM_PROMPT references route_files by name"
      pattern: "route_files"
    - from: "workspace/src/agent.py"
      to: "workspace/src/tools/search_in_file.py"
      via: "SYSTEM_PROMPT references search_in_file by name"
      pattern: "search_in_file"
---

<objective>
Rewrite the system prompt with corpus-specific retrieval rules and upgrade all tool docstrings with corpus structure hints.

Purpose: The root failure mode is bad retrieval choices — the agent doesn't look in adjacent-year bulletins. This plan hardcodes the FY Adjacency Rule and Parallel Search Rule into the system prompt, and adds corpus structure hints to every tool docstring so the LLM makes better retrieval decisions.

Output: Updated `agent.py` SYSTEM_PROMPT, updated docstrings in all 5 tool modules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-enhance-agent-prompts-tools-and-agentbeats-a2a-integration/05.1-RESEARCH.md
@workspace/src/agent.py
@workspace/src/tools/route_files.py
@workspace/src/tools/search_in_file.py
@workspace/src/tools/calculate.py
@workspace/src/tools/normalize_answer.py
@workspace/src/tools/verifier.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FY Adjacency Rule and Parallel Search Rule to SYSTEM_PROMPT</name>
  <files>workspace/src/agent.py</files>
  <action>
Edit the SYSTEM_PROMPT string in workspace/src/agent.py. Insert TWO new sections AFTER the existing "## Mandatory Planning Gate" section and BEFORE "## Scratch File Writing Instructions":

**Section 1: Fiscal Year Adjacency Rule (MANDATORY)**
```
## Fiscal Year Adjacency Rule (MANDATORY)

US Treasury bulletins are published monthly. FY data is often summarized
retrospectively in bulletins from the FOLLOWING calendar year.

Rule: For any question about FY N data:
1. Call route_files with the original question (routes to year N bulletins).
2. ALSO call route_files with "fiscal year {N+1}" to get year N+1 bulletins.
3. Search BOTH sets of files before concluding data is absent.

Example: FY1940 data may appear in the 1940-10 through 1941-09 bulletins,
not just the 1939-10 through 1940-09 bulletins.
```

**Section 2: Parallel Search Rule**
```
## Parallel Search Rule

When route_files returns 2 or more file paths, call search_in_file for
ALL relevant files in a SINGLE turn (parallel tool calls). Do not search
files one at a time — issue all search_in_file calls simultaneously.
This minimises the number of turns and finds the best evidence faster.
```

Do NOT add any "cannot determine" give-up logic — the verifier already handles this (per locked decision). Do NOT modify the existing Verification and Retry Protocol section. Do NOT change create_agent() or run_question().
  </action>
  <verify>
Read workspace/src/agent.py and confirm:
1. SYSTEM_PROMPT contains "Fiscal Year Adjacency Rule" section
2. SYSTEM_PROMPT contains "Parallel Search Rule" section
3. No "cannot determine" policy appears outside the existing Verification and Retry Protocol section
4. `python -c "from src.agent import SYSTEM_PROMPT; assert 'Fiscal Year Adjacency Rule' in SYSTEM_PROMPT; assert 'Parallel Search Rule' in SYSTEM_PROMPT; print('OK')"` from workspace/ directory
  </verify>
  <done>SYSTEM_PROMPT contains both retrieval rules; existing verification protocol is unchanged; module imports without error</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade tool docstrings with corpus structure hints</name>
  <files>
    workspace/src/tools/route_files.py
    workspace/src/tools/search_in_file.py
    workspace/src/tools/calculate.py
    workspace/src/tools/normalize_answer.py
    workspace/src/tools/verifier.py
  </files>
  <action>
Edit the docstring of the main public function in each tool module. Add corpus structure hints and usage guidance that the LLM will see when the tool is bound. Keep existing parameter documentation intact — add new lines above it.

**route_files.py** — Add to the `route_files()` function docstring, after the first summary line:
```
CORPUS STRUCTURE: Bulletins are named treasury_bulletin_{YEAR}_{MONTH:02d}.txt
covering January 1939 through September 2025 (697 files). FY N spans
October of year N-1 through September of year N.
IMPORTANT: FY N summaries are often published RETROSPECTIVELY in bulletins
from year N+1. Always call route_files a second time for year N+1 when
searching for FY N annual totals (e.g., for FY1940, also check 1941 bulletins).
Call this tool BEFORE search_in_file. Use returned paths as inputs to
search_in_file.
```

**search_in_file.py** — Add to the `search_in_file()` function docstring, after the first summary line:
```
CORPUS STRUCTURE: Each bulletin file contains markdown-formatted tables
delimited by pipe characters (|). Spans are 20-line windows; tables are
never split mid-row. Unit annotations appear as "(In millions of dollars)"
or similar text above table headers.

PARALLEL CALL GUIDANCE: When route_files returns multiple file paths,
call search_in_file for ALL files in a SINGLE turn (parallel tool calls).
Do NOT call sequentially — issue all calls simultaneously.

QUERY TIPS: Include the series name and year in your query (e.g.,
"defense expenditures 1940"). For FY totals, also try "fiscal year"
and "annual". Numeric-only queries fall back to regex automatically.
```

**calculate.py** — Add to the `calculate()` function docstring, after the first summary line:
```
CORPUS CONTEXT: Use this for ALL arithmetic on values extracted from
Treasury bulletins. Never compute arithmetic inline — always use this tool.
Supports standard operators (+, -, *, /, **, %) on integer/decimal literals.
```

Also add to `pct_change()` docstring after the first summary line:
```
CORPUS CONTEXT: Use this for ALL percent change calculations between
Treasury bulletin values. Never compute percent change inline.
```

Also add to `sum_values()` docstring after the first summary line:
```
CORPUS CONTEXT: Use this to sum multiple values extracted from Treasury
bulletin tables (e.g., monthly totals). Always include units from the
table header (e.g., "millions").
```

**normalize_answer.py** — Add to the `normalize_answer()` function docstring, after the first summary line:
```
USAGE: Call this ONLY after the verifier returns PASS and provides a
verification_token. The token is mandatory — calling without it raises
ValueError. Pass the raw calculated answer string and the token.
```

**verifier.py** — This is a subagent spec, not a directly-called tool. No docstring change needed on VERIFIER_SUBAGENT_SPEC. However, if there is a public helper function with a docstring, add:
```
CORPUS CONTEXT: The verifier checks evidence from Treasury bulletin
corpus files. It validates unit consistency (millions vs billions),
arithmetic correctness, evidence coverage, and answer format.
```
to `resolve_era_column_header()` if it has a docstring.

Do NOT change any function signatures, logic, or return types. Only edit docstrings.
  </action>
  <verify>
From workspace/ directory, run:
```bash
python -c "
from src.tools.route_files import route_files
from src.tools.search_in_file import search_in_file
from src.tools.calculate import calculate, pct_change, sum_values
from src.tools.normalize_answer import normalize_answer
assert 'CORPUS' in route_files.__doc__
assert 'PARALLEL' in search_in_file.__doc__
assert 'CORPUS' in calculate.__doc__
assert 'CORPUS' in pct_change.__doc__
assert 'CORPUS' in sum_values.__doc__
assert 'verification_token' in normalize_answer.__doc__
print('All docstrings updated')
"
```
  </verify>
  <done>All 5 tool modules have corpus structure hints in their public function docstrings; no function signatures or logic changed; all imports succeed</done>
</task>

</tasks>

<verification>
1. `python -c "from src.agent import SYSTEM_PROMPT, create_agent; print('agent OK')"` — agent module loads
2. `python -c "from src.tools.route_files import route_files; print(route_files.__doc__[:200])"` — docstring has hints
3. `cd workspace && python -m pytest tests/ -x --timeout=30 -k "not integration and not smoke"` — existing unit tests still pass (no logic changed)
</verification>

<success_criteria>
- SYSTEM_PROMPT has FY Adjacency Rule and Parallel Search Rule
- All 5 tool docstrings contain corpus structure hints
- No function signatures, logic, or return types changed
- Existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-enhance-agent-prompts-tools-and-agentbeats-a2a-integration/05.1-01-SUMMARY.md`
</output>
