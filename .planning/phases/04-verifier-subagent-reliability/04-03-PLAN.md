---
phase: 04-verifier-subagent-reliability
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - workspace/tests/test_verifier.py
  - workspace/tests/test_agent.py
  - workspace/tests/test_normalize_answer.py
autonomous: true

must_haves:
  truths:
    - "Unit tests pass for _generate_token, resolve_era_column_header, and normalize_answer token gate"
    - "Integration test test_smoke_verification_txt_is_stub is updated to expect real verification records instead of Phase 4 stub"
    - "All existing normalize_answer test calls pass a valid verification_token"
  artifacts:
    - path: "workspace/tests/test_verifier.py"
      provides: "Unit tests for verifier helper functions and normalize_answer gate"
      min_lines: 60
    - path: "workspace/tests/test_agent.py"
      provides: "Updated integration tests compatible with Phase 4 verification protocol"
      contains: "verification"
    - path: "workspace/tests/test_normalize_answer.py"
      provides: "All calls updated to normalize_answer(raw, token) two-arg form"
      contains: "verification_token"
  key_links:
    - from: "workspace/tests/test_verifier.py"
      to: "workspace/src/tools/verifier.py"
      via: "import _generate_token, resolve_era_column_header"
      pattern: "from src\\.tools\\.verifier import"
    - from: "workspace/tests/test_verifier.py"
      to: "workspace/src/tools/normalize_answer.py"
      via: "import normalize_answer for token gate tests"
      pattern: "from src\\.tools\\.normalize_answer import normalize_answer"
    - from: "workspace/tests/test_normalize_answer.py"
      to: "workspace/src/tools/normalize_answer.py"
      via: "update all 59 single-arg calls to two-arg form"
      pattern: "normalize_answer\\(raw,\\s*['\"].*['\"]\\)"
---

<objective>
Create unit tests for verifier helpers and update existing integration tests for Phase 4 compatibility.

Purpose: Validate VER-03 (token generation), VER-04 (normalize_answer gate), VER-06 (era resolver) with pure Python unit tests (no LLM). Update Phase 3 integration tests that now fail due to the verification_token requirement and the removal of the verification.txt stub.

Output: New `workspace/tests/test_verifier.py` with unit tests. Updated `workspace/tests/test_agent.py` with Phase 4-compatible assertions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-verifier-subagent-reliability/04-RESEARCH.md
@.planning/phases/04-verifier-subagent-reliability/04-01-SUMMARY.md
@.planning/phases/04-verifier-subagent-reliability/04-02-SUMMARY.md
@workspace/tests/test_agent.py
@workspace/src/tools/verifier.py
@workspace/src/tools/normalize_answer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_verifier.py with unit tests for verifier helpers and normalize_answer gate</name>
  <files>workspace/tests/test_verifier.py</files>
  <action>
Create `workspace/tests/test_verifier.py` with these test functions (all pure Python, no LLM calls):

**Token generation tests:**
1. `test_generate_token_returns_16_char_hex` — `_generate_token("19.14%")` returns a 16-character string matching `[0-9a-f]{16}`.
2. `test_generate_token_deterministic` — Same input always produces same output: `_generate_token("test") == _generate_token("test")`.
3. `test_generate_token_different_inputs` — Different inputs produce different tokens: `_generate_token("a") != _generate_token("b")`.

**Era-aware resolver tests:**
4. `test_resolve_era_header_exact_match` — `resolve_era_column_header("National defense", ["National defense", "Veterans"])` returns `"National defense"`.
5. `test_resolve_era_header_fuzzy_match` — `resolve_era_column_header("National defense and associated activities", ["National defense and related activities", "Veterans Administration"])` returns `"National defense and related activities"`.
6. `test_resolve_era_header_no_match` — `resolve_era_column_header("Completely unrelated", ["National defense", "Veterans"])` returns `None`.
7. `test_resolve_era_header_custom_cutoff` — With `cutoff=0.9`, a partially similar header that would match at 0.6 does not match. Use two strings that are similar but not identical enough for 0.9.

**Normalize_answer token gate tests:**
8. `test_normalize_answer_raises_on_empty_token` — `normalize_answer("test", "")` raises `ValueError` with message containing "verification_token".
9. `test_normalize_answer_raises_on_none_token` — `normalize_answer("test", None)` raises `ValueError` (or TypeError if None fails the type check — either is acceptable since it prevents bypass).
10. `test_normalize_answer_works_with_valid_token` — `normalize_answer("19.14%", "abc123")` returns `{"result": "19.14%"}`.
11. `test_normalize_answer_integer_with_token` — `normalize_answer("2,602", "validtoken")` returns `{"result": "2,602"}`.
12. `test_normalize_answer_invalid_input_still_returns_error` — `normalize_answer("", "validtoken")` returns `{"error": "INVALID_INPUT", ...}` (existing validation still works).

**Verifier spec structure tests:**
13. `test_verifier_spec_has_required_keys` — `VERIFIER_SUBAGENT_SPEC` has keys: `name`, `description`, `system_prompt`, `tools`.
14. `test_verifier_spec_name_is_verifier` — `VERIFIER_SUBAGENT_SPEC["name"] == "verifier"`.
15. `test_verifier_spec_tools_contains_calculate` — `VERIFIER_SUBAGENT_SPEC["tools"]` is a list containing the `calculate` function (import calculate from src.tools.calculate and check identity).
16. `test_verifier_system_prompt_no_uid_placeholder` — `"{uid}" not in VERIFIER_SUBAGENT_SPEC["system_prompt"]` (Pitfall 3).

Add sys.path setup at top: `sys.path.insert(0, str(Path(__file__).parent.parent))`.

All tests should be runnable with `cd workspace && python -m pytest tests/test_verifier.py -v`.
  </action>
  <verify>
Run `cd workspace && python -m pytest tests/test_verifier.py -v`. All 16 tests should pass.
  </verify>
  <done>
test_verifier.py has 16 passing unit tests covering token generation (deterministic, 16-char hex), era resolver (exact match, fuzzy match, no match, custom cutoff), normalize_answer gate (empty token, None token, valid token, existing validation preserved), and verifier spec structure (required keys, name, tools, no {uid} placeholder).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test_agent.py for Phase 4 compatibility</name>
  <files>workspace/tests/test_agent.py</files>
  <action>
Modify `workspace/tests/test_agent.py`:

1. **Update `test_smoke_verification_txt_is_stub`** — Rename to `test_smoke_verification_txt_has_records`. Change the assertion: instead of checking for "pending" or "Phase 4" stub content, verify that verification.txt exists and contains at least one verification attempt record. Look for indicators of a real verification record:
   - Assert `verification_path.exists()` (unchanged)
   - Read content and assert it contains at least one of: "PASS", "FAIL", "Status:", "Attempt" — any indicator that a real verification attempt was recorded (not the old stub)
   - Remove the assertion for "pending" or "phase 4" — those are gone
   - Keep the test as `@requires_vertex`, `@pytest.mark.integration`, `@pytest.mark.timeout(300)`

2. **Leave all other tests unchanged** — `test_smoke_question_produces_six_scratch_files`, `test_smoke_extracted_values_have_units`, `test_smoke_rerun_idempotent`, `test_smoke_write_todos_before_retrieval`, `test_model_switch_same_answer` all remain as-is. The agent now calls the verifier before normalize_answer, but the existing tests check scratch file completeness and ordering which should still work (the agent's behavior is richer but the observable outputs are the same).

3. **Update module docstring** — Change point 4 from "verification.txt contains stub content for Phase 4" to "verification.txt contains real verification records from verifier subagent".

Note: The normalize_answer tool's signature change (adding verification_token) does NOT break the integration tests because the agent LLM is the caller of normalize_answer (via tool call), and the updated SYSTEM_PROMPT tells it to pass the token. The tests call `run_question()` which invokes the agent, not `normalize_answer()` directly.
  </action>
  <verify>
Run `cd workspace && python -m pytest tests/test_agent.py --co -q` to verify test collection succeeds (no import errors). Confirm `test_smoke_verification_txt_has_records` appears in the collection list and `test_smoke_verification_txt_is_stub` does not.
  </verify>
  <done>
test_agent.py updated: test_smoke_verification_txt_is_stub renamed to test_smoke_verification_txt_has_records with assertions checking for real verification records (PASS/FAIL/Status/Attempt) instead of Phase 4 stub. Module docstring updated. All other tests unchanged. Test collection succeeds without import errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update all normalize_answer calls in test_normalize_answer.py to pass verification_token</name>
  <files>workspace/tests/test_normalize_answer.py</files>
  <action>
Modify `workspace/tests/test_normalize_answer.py` to pass a verification_token to every `normalize_answer()` call. Plan 04-02 changes the signature to `normalize_answer(raw: str, verification_token: str)` with no default, so every existing single-argument call will raise TypeError without this update.

Use `"test_token"` as the token value everywhere. There are 59 call sites across the file:

1. **Parametrized survey test (line 36)** — Change `normalize_answer(raw)` to `normalize_answer(raw, "test_token")`.

2. **TestInvalidInput class (6 calls)** — Update each call:
   - `normalize_answer(None)` -> `normalize_answer(None, "test_token")`
   - `normalize_answer("")` -> `normalize_answer("", "test_token")`
   - `normalize_answer("   ")` -> `normalize_answer("   ", "test_token")`
   - `normalize_answer(123)` -> `normalize_answer(123, "test_token")`
   - `normalize_answer(1.5)` -> `normalize_answer(1.5, "test_token")`
   - `normalize_answer(["a", "b"])` -> `normalize_answer(["a", "b"], "test_token")`

3. **TestUnicodeMinus class (4 calls)** — Add `"test_token"` as second argument to all four calls.

4. **TestWhitespaceStripping class (3 calls)** — Add `"test_token"` as second argument to all three calls.

5. **TestTrailingZerosPreserved class (6 calls)** — Add `"test_token"` as second argument to all six calls.

6. **TestPlainInteger class (5 calls)** — Add `"test_token"` as second argument to all five calls.

7. **TestIntegerComma class (4 calls)** — Add `"test_token"` as second argument to all four calls.

8. **TestPercentage class (7 calls)** — Add `"test_token"` as second argument to all seven calls.

9. **TestDollarPassThrough class (4 calls)** — Add `"test_token"` as second argument to all four calls.

10. **TestListPassThrough class (5 calls)** — Add `"test_token"` as second argument to all five calls.

11. **TestUnitWordPassThrough class (6 calls)** — Add `"test_token"` as second argument to all six calls.

12. **TestDatePassThrough class (2 calls)** — Add `"test_token"` as second argument to both calls.

13. **TestCommaDecimal class (1 call)** — Add `"test_token"` as second argument.

14. **TestReturnStructure class (5 calls)** — Update all five calls:
    - `normalize_answer("507")` -> `normalize_answer("507", "test_token")` (2 calls)
    - `normalize_answer(None)` -> `normalize_answer(None, "test_token")`
    - `normalize_answer("")` -> `normalize_answer("", "test_token")`
    - **test_never_raises loop** — Change `normalize_answer(bad_input)` to `normalize_answer(bad_input, "test_token")`. This is critical: without the token, every iteration would raise TypeError, causing the test to fail with "normalize_answer raised TypeError" for every input.

After all updates, the file should have zero calls matching the pattern `normalize_answer(something)` with a single argument — every call should be `normalize_answer(something, "test_token")`.
  </action>
  <verify>
Run `cd workspace && python -c "import ast, sys; tree = ast.parse(open('tests/test_normalize_answer.py').read()); calls = [n for n in ast.walk(tree) if isinstance(n, ast.Call) and hasattr(n.func, 'attr') and n.func.attr == 'normalize_answer']; single_arg = [c for c in calls if len(c.args) == 1]; print(f'Total calls: {len(calls)}, Single-arg calls: {len(single_arg)}'); sys.exit(1 if single_arg else 0)"`. Should print "Total calls: 59, Single-arg calls: 0" and exit 0.
  </verify>
  <done>
All 59 normalize_answer() calls in test_normalize_answer.py updated from single-arg to two-arg form with "test_token" as verification_token. Covers parametrized survey test, all class-based tests (InvalidInput, UnicodeMinus, WhitespaceStripping, TrailingZerosPreserved, PlainInteger, IntegerComma, Percentage, DollarPassThrough, ListPassThrough, UnitWordPassThrough, DatePassThrough, CommaDecimal, ReturnStructure), and the test_never_raises loop.
  </done>
</task>

</tasks>

<verification>
1. `cd workspace && python -m pytest tests/test_verifier.py -v` — all 16 tests pass
2. `cd workspace && python -m pytest tests/test_agent.py --co -q` — collection succeeds, no import errors
3. `test_smoke_verification_txt_has_records` exists, `test_smoke_verification_txt_is_stub` does not
4. No test imports or calls `normalize_answer` without a verification_token argument
5. `cd workspace && python -m pytest tests/test_normalize_answer.py -v` — all 59 tests pass with no TypeError from missing verification_token
</verification>

<success_criteria>
- 16 unit tests pass covering token generation, era resolver, token gate, and spec structure
- Integration test updated for Phase 4 verification records (no longer checks for stub)
- All 59 test_normalize_answer.py calls updated to two-arg form — no TypeError failures
- All tests collectible without import errors
- VER-03 (structured result with token) verified by spec structure tests
- VER-04 (token gate) verified by ValueError tests
- VER-06 (era resolver) verified by fuzzy match tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-verifier-subagent-reliability/04-03-SUMMARY.md`
</output>
